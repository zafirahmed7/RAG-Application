To Run First open terminal and run:
cd "FILE_PATH"

then run:
cd .\venv\

then run:
.\Scripts\activate

you should enter this:
(venv) PS "FILE_PATH""\venv>

to run program run:
python Program.py
